# Tamkin Application

Tamkin is a full-stack web application with a monorepo structure containing separate frontend and backend services. The application uses Docker Compose for orchestration and supports multiple environments (production and test) with PostgreSQL as the database.

The project appears to be in early development stages with basic scaffolding in place for both frontend and backend components.
